spring:
  application:
    name: Maintenance_App
  datasource:
    url: jdbc:postgresql://localhost:5432/residency_management
    username: postgres
    password: Pravin@69
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8081

